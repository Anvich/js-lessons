let arr = [5, 4, 3, 8, 0], mas=[], a, n=0;
function  filterFor(arr, a){
    for(i=0;i<arr.length;i++){
        if(arr[i]>=a){
            mas[n]=arr[i]
            n++
        }
    }
    return mas
}
filterFor(arr,5)
